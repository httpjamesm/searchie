<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Index | Searchie</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 min-h-screen">
    <div class="container mx-auto px-4 py-8">
        <div class="max-w-2xl mx-auto">
            <h1 class="text-4xl font-bold text-center mb-8 text-gray-500">
                {% if title %}
                {{ title }}
                {% else %}
                Searchie
                {% endif %}
            </h1>

            <form method="GET" action="/{{ dataset_id }}/search" class="mb-8">
                <div class="flex gap-2">
                    <input type="text" name="q" value="{{ query | default(value='') }}"
                        placeholder="Enter your search query..."
                        class="flex-1 px-4 py-2 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                        value="{{ query | default(value='') }}">
                    <button type="submit"
                        class="px-6 py-2 bg-blue-500 text-white rounded-lg hover:bg-blue-600 transition-colors">
                        Search
                    </button>
                </div>
            </form>

            {% if results %}
            <div class="space-y-4">
                {% for result in results %}
                <div class="bg-white p-4 rounded-lg shadow">
                    <strong>{{ result.datapoint.name }}</strong>
                    <p class="text-gray-800 whitespace-pre-wrap">{{ result.data }}</p>

                </div>
                {% endfor %}
            </div>
            {% elif query %}
            <div class="text-center text-gray-600">
                No results found for "{{ query }}"
            </div>
            {% endif %}
        </div>
    </div>
</body>

</html>